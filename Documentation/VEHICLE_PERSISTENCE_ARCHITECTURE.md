# Vehicle Persistence System - Architecture Reference

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    VEHICLE PERSISTENCE SYSTEM                   │
│                    (Ingenium Framework v1.0)                    │
└─────────────────────────────────────────────────────────────────┘

                          ┌──────────────────┐
                          │   CONFIGURATION  │
                          │  (_config/       │
                          │  vehicles.lua)   │
                          └────────┬─────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
    ┌───▼────────┐          ┌─────▼──────┐          ┌────────▼──┐
    │   CLIENT   │          │   SERVER   │          │ DATABASE  │
    │   LAYER    │          │   LAYER    │          │  LAYER    │
    └────────────┘          └────────────┘          └───────────┘
```

---

## Component Architecture

### CLIENT LAYER (`_vehicle_persistence.lua`)

```
┌──────────────────────────────────────────────────────┐
│           CLIENT VEHICLE PERSISTENCE                │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌─────────────────────────────────────────────┐   │
│  │  Vehicle Entry Detection Thread             │   │
│  │  ├─ Check player ped entity                 │   │
│  │  ├─ Get current vehicle                     │   │
│  │  ├─ Detect seat entry (any seat)           │   │
│  │  ├─ Check interaction duration              │   │
│  │  └─ Capture vehicle state                   │   │
│  └─────────────────────────────────────────────┘   │
│                        │                            │
│  ┌─────────────────────▼─────────────────────┐   │
│  │  Vehicle Condition Capture                │   │
│  │  ├─ Door states                           │   │
│  │  ├─ Window states                         │   │
│  │  ├─ Tire states                           │   │
│  │  ├─ Engine/body health                    │   │
│  │  └─ Lights status                         │   │
│  └─────────────────────┬─────────────────────┘   │
│                        │                            │
│  ┌─────────────────────▼─────────────────────┐   │
│  │  Server Event Trigger                     │   │
│  │  ├─ 'vehicle:persistence:register'       │   │
│  │  │   └─ plate, entity, seat, condition   │   │
│  │  └─ 'vehicle:persistence:exit'           │   │
│  │      └─ plate, coords, condition, fuel   │   │
│  └─────────────────────┬─────────────────────┘   │
│                        │                            │
│                        ├──► Server Processing      │
│                        │                            │
│                        └──► Listen for:            │
│                            'vehicle:persistence:  │
│                             registered'           │
│                                                    │
└──────────────────────────────────────────────────────┘
```

---

### SERVER LAYER (`_vehicle_persistence.lua`)

```
┌──────────────────────────────────────────────────────┐
│          SERVER VEHICLE PERSISTENCE MANAGER          │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌─────────────────────────────────────────────┐   │
│  │  Initialization Thread                      │   │
│  │  ├─ Load configuration                      │   │
│  │  ├─ Validate settings                       │   │
│  │  ├─ Start state tracker                     │   │
│  │  ├─ Start proximity manager                 │   │
│  │  ├─ Start batch updater                     │   │
│  │  └─ Start cleanup routine                   │   │
│  └─────────────────────┬───────────────────────┘   │
│                        │                            │
│  ┌─────────────────────▼───────────────────────┐   │
│  │  In-Memory Cache                            │   │
│  │  (ig.vehicleCache)                         │   │
│  │                                             │   │
│  │  {                                          │   │
│  │    [plate] = {                             │   │
│  │      entity = vehicleHandle,               │   │
│  │      owner = characterId|npcName,          │   │
│  │      lastCoords = vector3,                 │   │
│  │      lastCondition = table,                │   │
│  │      lastStatebag = table,                 │   │
│  │    }                                        │   │
│  │  }                                          │   │
│  └─────────────────────┬───────────────────────┘   │
│                        │                            │
│        ┌───────────────┼───────────────┐            │
│        │               │               │            │
│  ┌─────▼─────────┐ ┌──▼──────────┐ ┌─▼────────┐   │
│  │ State Tracker │ │ Proximity   │ │ Batch    │   │
│  │ Thread        │ │ Manager     │ │ Updater  │   │
│  │               │ │ Thread      │ │ Thread   │   │
│  │ Monitors:    │ │             │ │          │   │
│  │ ├─ Position  │ │ Spawns:    │ │ Collects:│   │
│  │ ├─ Damage    │ │ ├─ Within  │ │ ├─ Position │
│  │ ├─ Movement  │ │ │  range   │ │ ├─ Damage   │
│  │ └─ Changes   │ │ └─ Nearby  │ │ ├─ Fuel     │
│  │              │ │             │ │ └─ Mileage │
│  │ Updates      │ │ Despawns:  │ │            │
│  │ ig.vehicle   │ │ ├─ Beyond  │ │ Batches:  │
│  │ State        │ │ │  range   │ │ ├─ Every   │
│  │              │ │ └─ Old     │ │ │  10s     │
│  │              │ │  entities  │ │ ├─ Max 50  │
│  │              │ │            │ │ │  vehicles│
│  │              │ │            │ │ └─ Async   │
│  └──────────────┘ └────────────┘ └────────────┘   │
│        │                │              │            │
│        └────────────────┼──────────────┘            │
│                         │                            │
│  ┌──────────────────────▼──────────────────────┐   │
│  │  Event Handlers                             │   │
│  │  ├─ 'vehicle:persistence:register'          │   │
│  │  │   └─ RegisterVehicle()                   │   │
│  │  │       ├─ Add to cache                    │   │
│  │  │       ├─ Update database                 │   │
│  │  │       └─ Log interaction                 │   │
│  │  │                                           │   │
│  │  ├─ 'vehicle:persistence:exit'              │   │
│  │  │   └─ SaveVehicleState()                  │   │
│  │  │       ├─ Capture final state             │   │
│  │  │       ├─ Queue for batch update          │   │
│  │  │       └─ Clear interaction state         │   │
│  │  │                                           │   │
│  │  └─ Player Leave Event                      │   │
│  │      └─ OnPlayerLeave()                     │   │
│  │          ├─ Clear cache entries             │   │
│  │          ├─ Transfer ownership              │   │
│  │          └─ Mark despawned                  │   │
│  └──────────────────────┬──────────────────────┘   │
│                         │                            │
│  ┌──────────────────────▼──────────────────────┐   │
│  │  Cleanup Routine                            │   │
│  │  ├─ Every 60 seconds                        │   │
│  │  ├─ Find abandoned vehicles                 │   │
│  │  ├─ Check timeouts                          │   │
│  │  ├─ Delete from database                    │   │
│  │  └─ Log cleanup results                     │   │
│  └─────────────────────────────────────────────┘   │
│                                                      │
└──────────────────────────────────────────────────────┘
```

---

### DATABASE LAYER

```
┌──────────────────────────────────────────────────────────┐
│              DATABASE SCHEMA                             │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────────────────────────────────────┐       │
│  │  vehicles (Extended)                        │       │
│  │  ├─ ID (PK)                                 │       │
│  │  ├─ Plate (UNIQUE)                          │       │
│  │  ├─ Character_ID (FK - Player)              │       │
│  │  ├─ Model, Coords, Condition                │       │
│  │  ├─ vehicle_type (owned|npc|world)          │       │
│  │  ├─ npc_owner (NPC name)                    │       │
│  │  ├─ persistent (bool)                       │       │
│  │  ├─ statebag_data (xvehicle props)          │       │
│  │  ├─ liveries (paint/livery data)            │       │
│  │  ├─ despawned (bool)                        │       │
│  │  ├─ despawn_time (timestamp)                │       │
│  │  ├─ last_interaction (timestamp)            │       │
│  │  ├─ interaction_count (int)                 │       │
│  │  └─ Indexes on:                             │       │
│  │      vehicle_type, persistent, despawned    │       │
│  └─────────────────────────────────────────────┘       │
│              │                  │                       │
│              │                  │                       │
│  ┌───────────▼──────────┐  ┌───▼──────────────────┐   │
│  │ vehicle_persistence │  │ vehicle_interactions │   │
│  │ _state              │  │                      │   │
│  │                     │  │ ├─ ID (PK)           │   │
│  │ ├─ ID (PK)          │  │ ├─ plate (FK)        │   │
│  │ ├─ plate (UNIQUE,FK)│  │ ├─ player_id         │   │
│  │ ├─ spawned_entity   │  │ ├─ character_id      │   │
│  │ │  _id              │  │ ├─ interaction_type  │   │
│  │ ├─ current_coords   │  │ ├─ timestamp         │   │
│  │ ├─ last_check       │  │ ├─ vehicle_condition │   │
│  │ ├─ movement_detected│  │ └─ Indexes on:       │   │
│  │ ├─ damage_detected  │  │     plate, timestamp │   │
│  │ ├─ in_cache         │  │                      │   │
│  │ ├─ player_in_vehicle│  │ (Audit Trail)        │   │
│  │ └─ owner_identifier │  │                      │   │
│  │                     │  │                      │   │
│  │ (Runtime Tracking)  │  │ (Interaction Log)    │   │
│  └─────────────────────┘  └──────────────────────┘   │
│                                                        │
└──────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

### Vehicle Registration Flow

```
┌─────────────────────────────────────────────────────────────┐
│ VEHICLE REGISTRATION FLOW                                   │
├─────────────────────────────────────────────────────────────┤

┌──────────┐
│  PLAYER  │
│ ENTERS   │
│ VEHICLE  │
└────┬─────┘
     │
     │ GetVehiclePedIsIn()
     ▼
┌─────────────────────────────┐
│ CLIENT DETECTION THREAD     │
│ ├─ Detect seat entry        │
│ ├─ Wait minInteractionTime  │
│ ├─ Validate player still in │
│ ├─ Verify vehicle valid     │
│ └─ Get plate                │
└────┬────────────────────────┘
     │
     │ Capture Condition
     │ ├─ Door states (0-7)
     │ ├─ Window states (0-3)
     │ ├─ Tire states (0-5)
     │ ├─ Panel states (0-19)
     │ └─ Health values
     ▼
┌─────────────────────────────┐
│ TRIGGER SERVER EVENT        │
│ TriggerServerEvent(         │
│  'vehicle:persistence:      │
│   register',                │
│  plate,                     │
│  entity,                    │
│  seat,                      │
│  condition                  │
│ )                           │
└────┬────────────────────────┘
     │
     ▼
SERVER RECEIVES EVENT

┌──────────────────────────────┐
│ EVENT HANDLER                │
│ RegisterVehicle()            │
└────┬─────────────────────────┘
     │
     ├─► Add to ig.vehicleCache
     │   [plate] = {
     │     entity = vehicleEntity,
     │     owner = characterId,
     │     lastCoords = coords,
     │     lastCondition = condition,
     │   }
     │
     ├─► ig.sql.veh.RegisterPersistent()
     │   ├─ UPDATE persistent = 1
     │   ├─ SET vehicle_type = 'npc'
     │   ├─ SET npc_owner = npcName
     │   ├─ SET last_interaction = NOW()
     │   └─ Increment interaction_count
     │
     ├─► ig.sql.veh.LogInteraction()
     │   ├─ INSERT INTO vehicle_interactions
     │   ├─ Log plate, player_id, type='registered'
     │   └─ Store condition snapshot
     │
     ├─► ig.sql.veh.UpdatePersistenceState()
     │   ├─ INSERT/UPDATE vehicle_persistence_state
     │   ├─ SET spawned_entity_id = entityHandle
     │   ├─ SET current_coords = coords
     │   └─ SET owner_identifier = characterId
     │
     └─► TriggerClientEvent('vehicle:persistence:registered')
         └─ Client shows notification

DATABASE UPDATED ✓
CACHE POPULATED ✓
VEHICLE NOW PERSISTENT ✓
```

---

### Spawn/Despawn Flow

```
┌──────────────────────────────────────────────────┐
│ PROXIMITY MANAGEMENT FLOW                        │
├──────────────────────────────────────────────────┤

EVERY spawnTimeout (10s):

┌─────────────────────────────────────┐
│ GET ALL PERSISTENT VEHICLES         │
│ FROM DATABASE                       │
│ WHERE persistent = 1                │
└────┬────────────────────────────────┘
     │
     ├─────FOR EACH VEHICLE─────┐
     │                           │
     │  ┌──────────────────────┐ │
     │  │ GET ALL PLAYERS      │ │
     │  │ ONLINE               │ │
     │  └──────────────────────┘ │
     │           │               │
     │           ├─FOR EACH PLAYER─┐
     │           │                 │
     │           │ CALCULATE DISTANCE
     │           │ FROM VEHICLE    │
     │           │                 │
     │           │ IF distance <   │
     │           │ spawnDistance   │
     │           │ (200m)          │
     │           │  SPAWN = TRUE   │
     │           │ ELSE            │
     │           │  SPAWN = FALSE  │
     │           │                 │
     │     ┌─────▼────────┐        │
     │     │ SPAWN        │        │
     │     │ VEHICLE?     │        │
     │     ├──────────────┤        │
     │     │ YES │   NO   │        │
     │     └──┬──┴────┬───┘        │
     │        │       │           │
     │  ┌─────▼───┐  │            │
     │  │ Vehicle │  │            │
     │  │ Already │  │            │
     │  │ Spawned?│  │            │
     │  └─────────┘  │            │
     │     NO YES    │            │
     │      │  │     │            │
     │      ▼  │     │            │
     │  SPAWN  SKIP  │            │
     │  IT!    IT    │            │
     │        │      │            │
     │        └──┬───┘            │
     │           │                │
     └───────────┤────────────────┘
                 │
DESPAWN CHECK:
                 │
                 ├─────FOR EACH CACHED VEHICLE─────┐
                 │                                  │
                 │  ┌──────────────────────────┐   │
                 │  │ GET ALL PLAYERS          │   │
                 │  │ ONLINE                   │   │
                 │  └──────────────────────────┘   │
                 │           │                     │
                 │           │                     │
                 │  CALCULATE MIN DISTANCE         │
                 │  FROM ALL PLAYERS               │
                 │           │                     │
                 │  IF minDistance >               │
                 │  despawnDistance                │
                 │  (400m)                         │
                 │  DESPAWN = TRUE                 │
                 │  ELSE                           │
                 │  DESPAWN = FALSE                │
                 │           │                     │
                 │     ┌─────▼──────┐              │
                 │     │ DESPAWN    │              │
                 │     │ VEHICLE?   │              │
                 │     └─────┬──────┘              │
                 │           │                     │
                 │      YES / NO                   │
                 │      /       \                  │
                 │  ┌──▼──┐  ┌──▼──┐              │
                 │  │DELETE│ │KEEP │              │
                 │  │FROM  │ │IN   │              │
                 │  │WORLD │ │WORLD│              │
                 │  └──────┘ └─────┘              │
                 │                                  │
                 └──────────────────────────────────┘
```

---

## State Tracking System

```
┌──────────────────────────────────────────┐
│ STATE TRACKING CYCLE (Every 5-10s)      │
├──────────────────────────────────────────┤

FOR EACH CACHED VEHICLE:

┌────────────────────────────┐
│ CHECK VEHICLE EXISTS       │
│ DoesEntityExist(entity)    │
└────┬───────────────────────┘
     │
     ├─ YES ──┬── NO ──┐
     │        │        │
     │   ┌────▼──┐  ┌──▼─────────┐
     │   │Continue│  │Clear entity│
     │   │checks  │  │from cache  │
     │   └────┬──┘  └────────────┘
     │        │
     │   ┌────▼──────────────────┐
     │   │ GET CURRENT POSITION  │
     │   │ GetEntityCoords()     │
     │   └────┬─────────────────┘
     │        │
     │   ┌────▼──────────────────┐
     │   │ COMPARE WITH LAST     │
     │   │ POSITION              │
     │   │ Distance calc:        │
     │   │ #(current -last)      │
     │   └────┬─────────────────┘
     │        │
     │     DISTANCE >
     │     movementThreshold
     │     (5.0m)?
     │        │
     │   YES / NO
     │   /      \
     │ ┌─▼──────┐ ┌──────┐
     │ │MOVEMENT│ │NO    │
     │ │DETECTED│ │MOVE  │
     │ │        │ │      │
     │ │Queue   │ └──────┘
     │ │update  │
     │ └────┬──┘
     │      │
     │  ┌───▼───────────────┐
     │  │ IF TRACK DAMAGE   │
     │  │ ENABLED:          │
     │  │ GetVehicleCondition()
     │  └───┬───────────────┘
     │      │
     │  ┌───▼──────────────────┐
     │  │ COMPARE WITH LAST    │
     │  │ CONDITION            │
     │  │ (doors, health, etc) │
     │  └───┬──────────────────┘
     │      │
     │   CHANGED?
     │      │
     │  YES / NO
     │  /      \
     │┌─▼──────┐ ┌──────┐
     ││DAMAGE  │ │NO    │
     ││DETECTED│ │DAMAGE│
     ││        │ │      │
     ││Queue   │ └──────┘
     ││update  │
     │└────┬──┘
     │     │
     │  ┌──▼────────────────────┐
     │  │ QUEUE STATE CHANGES   │
     │  │ TO ig.vehicleState:   │
     │  │ {                     │
     │  │   plate: plate,       │
     │  │   coords: coords,     │
     │  │   condition: cond,    │
     │  │   fuel: fuel,         │
     │  │   timestamp: timer    │
     │  │ }                     │
     │  └──────────────────────┘
     │
     └─ REPEAT FOR NEXT VEHICLE
```

---

## Batch Update System

```
┌────────────────────────────────────┐
│ BATCH UPDATE CYCLE                 │
│ (Every 10 seconds)                 │
├────────────────────────────────────┤

STATE CHANGES QUEUE:
ig.vehicleState = {
    [plate1] = { coords: ..., condition: ... },
    [plate2] = { coords: ..., damage: ... },
    [plate3] = { fuel: ..., mileage: ... },
    ...
}

┌────────────────────────────────┐
│ BATCH UPDATE THREAD            │
│ ├─ Wait 10 seconds             │
│ ├─ Check ig.vehicleState      │
│ ├─ Build batch (max 50)        │
│ └─ Send to database            │
└────┬───────────────────────────┘
     │
     ├─FOR EACH QUEUED STATE:
     │
     │  IF coords:
     │   └─ ig.sql.veh.UpdatePosition()
     │
     │  IF condition:
     │   └─ ig.sql.veh.UpdateVehicleState()
     │
     │  IF fuel/mileage:
     │   └─ ig.sql.veh.UpdateVehicleStats()
     │
     │  IF statebag:
     │   └─ UPDATE statebag_data
     │
     ├─ Clear ig.vehicleState[plate]
     │
     └─ Log batch result
```

---

## Cleanup Routine

```
┌──────────────────────────────────┐
│ CLEANUP ROUTINE                  │
│ (Every 60 seconds)               │
├──────────────────────────────────┤

CHECK ABANDONED VEHICLES:

┌──────────────────────────────┐
│ FIND NPC VEHICLES            │
│ WHERE:                       │
│ vehicle_type = 'npc'         │
│ AND persistent = 0           │
│ AND last_interaction <       │
│     (NOW - 30 minutes)       │
└──┬───────────────────────────┘
   │
   ├─ DELETE FROM vehicles
   │
   └─ Log: "Deleted X NPC vehicles"

┌──────────────────────────────┐
│ FIND WORLD VEHICLES          │
│ WHERE:                       │
│ vehicle_type = 'world'       │
│ AND persistent = 0           │
│ AND last_interaction <       │
│     (NOW - 7 days)           │
└──┬───────────────────────────┘
   │
   ├─ DELETE FROM vehicles
   │
   └─ Log: "Deleted X world vehicles"

┌──────────────────────────────┐
│ CHECK MAX VEHICLES           │
│ COUNT WHERE persistent = 1   │
│ IF count > maxTrackedVehicles│
│ (500):                       │
└──┬───────────────────────────┘
   │
   ├─ DELETE oldest parked
   │  vehicles by last_interaction
   │
   └─ Log: "Cleaned up X vehicles"
```

---

## Configuration Hierarchy

```
┌─────────────────────────────────────────┐
│ CONFIGURATION HIERARCHY                 │
├─────────────────────────────────────────┤

_config/vehicles.lua
    │
    ├─ conf.persistence (MASTER)
    │   │
    │   ├─ Global toggles
    │   │   ├─ enablePersistence
    │   │   └─ enableNpcVehiclePersistence
    │   │
    │   ├─ Spawn/Despawn
    │   │   ├─ spawnDistance (200m)
    │   │   ├─ despawnDistance (400m)
    │   │   ├─ spawnTimeout (10s)
    │   │   └─ maxSpawnsPerTick (5)
    │   │
    │   ├─ Parking
    │   │   ├─ despawnWhenParked (false)
    │   │   ├─ persistWhileParked (true)
    │   │   └─ updateCoordsIfChanged (true)
    │   │
    │   ├─ StateTracking
    │   │   ├─ trackDamage (true)
    │   │   ├─ trackLiveries (true)
    │   │   ├─ damageCheckInterval (5s)
    │   │   └─ movementThreshold (5m)
    │   │
    │   ├─ Cleanup
    │   │   ├─ enabled (true)
    │   │   ├─ runInterval (60s)
    │   │   ├─ abandonedVehicleTimeout (7 days)
    │   │   └─ npcVehicleTimeout (30 min)
    │   │
    │   ├─ DatabaseSync
    │   │   ├─ batchUpdateInterval (10s)
    │   │   ├─ maxBatchSize (50)
    │   │   └─ asyncUpdates (true)
    │   │
    │   ├─ Interactions
    │   │   ├─ detectSeatEntry (true)
    │   │   └─ minInteractionDuration (1s)
    │   │
    │   ├─ Logging
    │   │   ├─ enabled (true)
    │   │   ├─ logLevel ("info")
    │   │   ├─ logSpawns (true)
    │   │   ├─ logDespawns (true)
    │   │   └─ logInteractions (true)
    │   │
    │   ├─ Types (Vehicle-specific)
    │   │   ├─ owned { persistent=true, ... }
    │   │   ├─ npc { persistent=false, ... }
    │   │   └─ world { persistent=false, ... }
    │   │
    │   ├─ PersistStatebag
    │   │   ├─ "xvehicle"
    │   │   ├─ "vehicle_customization"
    │   │   └─ "vehicle_meta"
    │   │
    │   └─ Dev
    │       ├─ debugMode (false)
    │       ├─ disableCleanup (false)
    │       └─ logAllUpdates (false)
    │
    └─ Used by:
        ├─ Server: _vehicle_persistence.lua
        ├─ Client: _vehicle_persistence.lua
        └─ SQL: [SQL]/_vehicles.lua
```

---

This architecture documentation provides a complete visual reference for how the Vehicle Persistence System operates across all layers.
